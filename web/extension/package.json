{
  "name": "axon-extension",
  "version": "0.1.0",
  "scripts": {
    "dev": "esbuild content.ts background.ts --bundle --outdir=. --format=iife --target=es2020 --watch",
    "build": "esbuild content.ts background.ts --bundle --outdir=. --format=iife --target=es2020"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "@types/firefox-webext-browser": "^120.0.4",
    "esbuild": "^0.19.0",
    "webextension-polyfill": "^0.12.0"
  }
}
