{
  "name": "axon-extension",
  "version": "0.1.0",
  "scripts": {
    "dev": "esbuild content.ts --bundle --outfile=content.js --watch",
    "build": "esbuild content.ts --bundle --outfile=content.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}